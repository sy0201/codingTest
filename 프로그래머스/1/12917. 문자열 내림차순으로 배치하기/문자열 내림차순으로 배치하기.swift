func solution(_ s:String) -> String {
    // let sortedArray = s.sorted(by: >)
    // let result = String(sortedArray)
    // return result
    
    let sortedArray = s.sorted(by: >)
    let result = String(sortedArray)
    return result
}